server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
    username: ${SPRING_DATASOURCE_USERNAME:postgres.jelihhaimwbeymculhsm}
    password: ${SPRING_DATASOURCE_PASSWORD:@#C2QWe4gte}
    driver-class-name: org.postgresql.Driver

  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: mysupersecurejwtsecretkeyformicroservicessystemproject2026
  expiration: 86400000

logging:
  level:
    com.sms: DEBUG
    org.springframework.security: DEBUG